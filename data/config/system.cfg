; Load the user-specific config domain
System.UserConfig = true
; Minimum elapsed ticks per second for one iteration in the runloop.
; If elapsed ticks is less then this number then the runloop will sleep
; until time matches.
System.MinimumElapsedTicks = 0

