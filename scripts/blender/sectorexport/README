sectorexport.py: Description

Is a Blender script for exporting to Crystal space map format, it assumes
an entry in vfs.cfg like this:

     VFS.Mount.lev/blend    = $@data$/flarge$/exported.zip

After exporting your level, invoke walktest with the blend level like:

       ./walktest blend

