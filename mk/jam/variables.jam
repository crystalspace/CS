#============================================================================
# Setup several global variables for the build
#============================================================================

# make sure these variables have a value
MODE ?= "" ;

# rename some vars
CCFLAGS += $(COMPILER.CFLAGS) ;
C++FLAGS += $(COMPILER.CFLAGS) $(COMPILER.C++FLAGS) ;
LINKLIBS += $(COMPILER.LFLAGS) ;

