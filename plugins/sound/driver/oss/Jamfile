SubDir TOP plugins sound driver oss ;

if $(OSS.AVAILABLE) = "yes" && $(TARGET.OS) != "WIN32" 
{
Help ossdrv : "Crystal Space OSS sound driver" ;

Plugin ossdrv
	: [ Wildcard *.cpp *.h ]
;
LinkWith ossdrv : csutil ;
ExternalLibs ossdrv :  OSS ;

CompileGroups ossdrv :  sounddrivers ;

}

