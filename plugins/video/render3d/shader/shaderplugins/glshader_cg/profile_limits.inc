// Tabularize what profiles use what limits
PROFILE_BEGIN(ARBVP1)
  LIMIT(MaxAddressRegs, MAX_PROGRAM_NATIVE_ADDRESS_REGISTERS_ARB)
  LIMIT(MaxInstructions, MAX_PROGRAM_NATIVE_INSTRUCTIONS_ARB)
  LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB)
  LIMIT(NumTemps, MAX_PROGRAM_TEMPORARIES_ARB)
PROFILE_END(ARBVP1)

PROFILE_BEGIN(ARBFP1)
  LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB)
  LIMIT(MaxTexIndirections, MAX_PROGRAM_NATIVE_TEX_INDIRECTIONS_ARB)
  LIMIT(NumInstructionSlots, MAX_PROGRAM_NATIVE_INSTRUCTIONS_ARB)
  LIMIT(NumMathInstructionSlots, MAX_PROGRAM_NATIVE_ALU_INSTRUCTIONS_ARB)
  LIMIT(NumTemps, MAX_PROGRAM_TEMPORARIES_ARB)
  LIMIT(NumTexInstructionSlots, MAX_PROGRAM_NATIVE_TEX_INSTRUCTIONS_ARB)
PROFILE_END(ARBFP1)

PROFILE_BEGIN(VP20)
  //LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB) // - spec 'hardcodes' 96
PROFILE_END(VP20)

PROFILE_BEGIN(VP30)
  LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB)
PROFILE_END(VP30)

PROFILE_BEGIN(VP40)
  LIMIT(MaxAddressRegs, MAX_PROGRAM_NATIVE_ADDRESS_REGISTERS_ARB)
  LIMIT(MaxInstructions, MAX_PROGRAM_NATIVE_INSTRUCTIONS_ARB)
  LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB)
  LIMIT(NumTemps, MAX_PROGRAM_TEMPORARIES_ARB)
PROFILE_END(VP40)

PROFILE_BEGIN(FP30)
  LIMIT(NumInstructionSlots, MAX_PROGRAM_NATIVE_INSTRUCTIONS_ARB)
  LIMIT(NumTemps, MAX_PROGRAM_TEMPORARIES_ARB)
PROFILE_END(FP30)

PROFILE_BEGIN(FP40)
  LIMIT(MaxLocalParams, MAX_PROGRAM_LOCAL_PARAMETERS_ARB)
  LIMIT(NumInstructionSlots, MAX_PROGRAM_NATIVE_INSTRUCTIONS_ARB)
  LIMIT(NumTemps, MAX_PROGRAM_TEMPORARIES_ARB)
PROFILE_END(FP40)
